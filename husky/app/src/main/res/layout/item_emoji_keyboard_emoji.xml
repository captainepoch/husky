<?xml version="1.0" encoding="utf-8"?><!-- not a best way but I'm lazy to do proper button -->
<androidx.emoji2.widget.EmojiButton xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/emoji_keyboard_emoji"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:layout_margin="0dp"
    android:padding="0dp"
    android:lines="1"
    android:minWidth="0dp"
    android:textAlignment="center"
    android:textColor="?android:attr/textColorPrimary"
    android:textSize="?attr/status_text_medium"
    app:backgroundTint="@color/emoji_reaction_button"
    tools:text="A" />
